model = "gpt-5.5"
model_reasoning_effort = "high"

approval_policy = "on-request"
sandbox_mode = "workspace-write"

# Mantener búsquedas web restringidas por defecto.
web_search = "cached"

[sandbox_workspace_write]
network_access = true
